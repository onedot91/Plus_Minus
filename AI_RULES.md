# AI_RULES

## 프로젝트 한 줄 설명

이 프로젝트는 초등학생이 덧셈과 뺄셈을 게임처럼 연습할 수 있게 만든 React + Vite 기반 수학 학습 앱이다. 교사가 비전공자로 AI와 함께 조금씩 고쳐 가는 프로젝트이므로, 코드는 완벽한 설계보다 이해 가능하고 안전한 변경을 우선한다.

## 주요 사용자

- 초등학생: 문제를 풀며 캐릭터, 스테이지, 보상으로 재미를 느끼는 사용자
- 초등학교 교사: 수업이나 과제에 맞게 문제, 난이도, 문구, 이미지를 조정하는 관리자 역할
- AI 코딩 도우미: 기존 구조를 깨지 않고 필요한 부분을 작게 수정하는 협업자

## 현재 기술 구조

- 프론트엔드: React 19, TypeScript, Vite
- 스타일: Tailwind CSS 4와 `src/index.css`의 전역 테마 규칙
- 아이콘: `lucide-react`
- 애니메이션: `motion`
- 사운드: `howler`
- 실행 명령:
  - 개발 서버: `npm run dev`
  - 빌드 확인: `npm run build`
  - 타입 확인: `npm run lint`

## 작업 전 확인 규칙

1. 먼저 `package.json`, `src/App.tsx`, `src/index.css`, 관련 컴포넌트를 확인한다.
2. 디자인을 바꾸는 작업이면 `DESIGN.md`와 현재 화면 스타일을 먼저 확인한다.
3. 이미지나 캐릭터를 추가할 때는 `src/assets`의 기존 파일명 규칙을 참고한다.
4. 큰 구조를 바꾸기 전에, 같은 목표를 더 작은 수정으로 해결할 수 있는지 먼저 본다.

## 제품 원칙

- 이 앱의 핵심은 "수학 문제를 재미있게 반복 연습하게 하는 것"이다.
- 기능을 추가할 때는 학생이 다음 행동을 바로 알 수 있어야 한다.
- 교사가 나중에 문제, 문구, 난이도를 바꾸기 쉬워야 한다.
- 게임 요소는 학습을 방해하지 말고 학습 동기를 높이는 방향으로만 사용한다.
- 실패, 오답, 잠금 상태의 문구는 학생을 탓하지 않는 부드러운 표현을 사용한다.

## 디자인 원칙

- 현재 앱은 어두운 배경, 밝은 글자, 초록색 강조색을 쓰는 Spotify식 테마를 따른다.
- 새 화면도 가능한 한 `src/index.css`의 `.spotify-app` 테마 안에서 자연스럽게 보이게 만든다.
- 버튼은 둥근 pill 형태를 기본으로 하고, 주요 행동은 초록색 강조를 사용한다.
- 캐릭터와 스테이지 이미지가 주인공이므로 UI 장식은 과하게 늘리지 않는다.
- 초등학생이 쓰는 화면이므로 글자는 작게 숨기지 말고, 버튼은 클릭하기 충분히 크게 만든다.
- 모바일과 태블릿에서도 글자 겹침, 버튼 잘림, 이미지 과도한 확대가 없도록 확인한다.

## 개발 원칙

- TypeScript 타입 오류가 생기지 않게 작성한다.
- 기존 파일과 함수 이름, 데이터 구조를 최대한 유지한다.
- 한 번에 많은 리팩터링을 하지 말고, 요청한 목표에 필요한 만큼만 고친다.
- `src/App.tsx`가 이미 큰 파일이므로, 새 기능이 길어지면 작고 명확한 컴포넌트나 helper로 분리한다.
- 같은 데이터가 반복될 때는 배열이나 객체로 정리하되, 교사가 읽기 어렵게 과한 추상화는 하지 않는다.
- 사용하지 않는 import, 변수, 이미지 참조는 남기지 않는다.
- 화면 문구는 가능하면 한국어로 자연스럽게 작성한다.
- 비밀키나 개인 정보는 코드에 직접 넣지 않는다. 필요한 값은 `.env` 계열 파일을 사용한다.

## 학습 콘텐츠 규칙

- 문제 난이도는 초등학생 기준으로 너무 급격히 올라가지 않게 한다.
- 덧셈, 뺄셈 문제는 정답이 명확해야 하며, 음수나 교육과정 밖의 내용이 섞이지 않게 주의한다.
- 새 문제 풀을 만들 때는 중복 문제와 지나치게 비슷한 문제가 너무 많지 않은지 확인한다.
- 단원, 스테이지, 레벨 이름은 교사가 나중에 찾아 고치기 쉬운 형태로 둔다.

## 에셋 규칙

- 실제 앱에서 쓰는 이미지는 `src/assets`에 둔다.
- 임시 생성물이나 후보 이미지는 `tmp` 아래에 둘 수 있지만, 앱 코드에서 직접 참조하지 않는다.
- 새 이미지 파일명은 역할이 드러나게 작성한다. 예: `player-teacher-default.png`, `unit1-level3-name-attack.webp`
- 이미지 교체 시 default, attack, hit, defeat 같은 상태별 세트가 깨지지 않는지 확인한다.

## 검증 규칙

작업 후 가능한 범위에서 아래를 확인한다.

1. `npm run lint`
2. `npm run build`
3. 화면 변경이 있으면 `npm run dev`로 실제 화면 확인

검증을 실행하지 못했다면, 어떤 검증을 못 했는지 마지막 응답에 분명히 적는다.

## AI 협업 규칙

- 교사가 이해할 수 있게 변경 이유를 짧고 쉬운 말로 설명한다.
- 어려운 기술 용어는 꼭 필요할 때만 쓰고, 쓰면 간단히 풀어 설명한다.
- 불확실한 부분은 추측으로 크게 바꾸지 말고, 먼저 관련 코드를 확인한다.
- 기존 사용자 변경사항을 함부로 되돌리지 않는다.
- 최종 답변에는 바꾼 파일, 핵심 변경 내용, 실행한 확인 명령을 간단히 정리한다.
